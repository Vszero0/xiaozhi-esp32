- 硬件板卡通过继承 `Board` 抽象类并在文件末尾使用 `DECLARE_BOARD(YourBoard)` 宏注册到 `create_board()` 工厂，由链接期选择具体实现
- 跨任务通信统一采用 `xEventGroupSetBits` 触发 `MAIN_EVENT_*` 常量，主循环在 `Application::Run()` 中集中处理
- 对外服务（Application/McpServer/Board）均以静态单例 `GetInstance()` 暴露全局访问点